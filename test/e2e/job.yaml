apiVersion: batch/v1
kind: Job
metadata:
  name: curl
spec:
  template:
    spec:
      containers:
      - name: curl
        image: ghcr.io/cybozu/ubuntu-debug:22.04
        command: ["curl", "-i", "placeholder"]
      restartPolicy: Never
  backoffLimit: 2
