image:
  repository: controller
  pullPolicy: IfNotPresent
  reference: latest

controller:
  overwriteMBCSchedule: "* * * * *"
  gcInterval: 1s
  env:
    - name: REQUEUE_RECONCILIATION_AFTER
      value: "10s"
