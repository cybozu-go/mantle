image:
  repository: controller
  pullPolicy: IfNotPresent
  tag: latest

controller:
  overwriteMBCSchedule: "* * * * *"
  gcInterval: 1s
  env:
    - name: REQUEUE_RECONCILIATION_AFTER
      value: "10s"
