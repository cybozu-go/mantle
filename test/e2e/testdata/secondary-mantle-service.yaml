apiVersion: v1
kind: Service
metadata:
  name: mantle
  namespace: rook-ceph
spec:
  type: NodePort
  ports:
  - port: 58080
    targetPort: 58080
    protocol: TCP
    name: grpc
  selector:
    app.kubernetes.io/instance: mantle
    app.kubernetes.io/name: mantle
