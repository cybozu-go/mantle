apiVersion: v1
kind: Service
metadata:
  name: my-nginx-np
  namespace: test
  labels:
    run: my-nginx
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: my-nginx
