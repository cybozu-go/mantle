apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "%s"
  name: "%s"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: "%s"
  resources:
    requests:
      storage: "%s"
  storageClassName: "%s"
